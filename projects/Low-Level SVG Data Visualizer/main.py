# Low-Level SVG Data Visualizer
# Reads a dataset, calculates linear scales, and directly manipulates XML DOM elements to generate a custom SVG bar chart with inline CSS styling, saving the output as a standalone HTML file.
